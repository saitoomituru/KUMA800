# [下書き] Season 1 Wave 2–7 実装レシート

- 記録日：2026-08-11 JST
- 対象：KUMA800 Season 1 fake vertical slice
- 実行agent：Codex（GPT-5系。API上の内部snapshot識別子は取得不能）
- host：macOS Darwin 24.6.0、x86_64
- runtime：Python 3.12.7、Huey 2.6.0、FastMCP 3.4.7、SQLite
- 関連Issue：[Issue #2](https://github.com/saitoomituru/KUMA800/issues/2)

## 観測事実

### [FACT] Wave 2 保存境界

- `kuma.sqlite3`にmigration、source、fetch run、観測、主張、source stateを実装した。
- 観測と主張はSQLite triggerでもUPDATE／DELETEを拒否する。
- application writeは`ObservationIngestStore`へ限定し、read-only接続はSQLiteの`mode=ro`と`query_only`を使う。
- 同じsource event IDと内容hashの再ingestは同じ観測へ収束する。上流内容hashが変われば別観測として残す。
- `users.yaml`は観測DBと分離し、OS標準user data directoryを既定とする。`KUMA800_DATA_DIR`を指定したservice群とは同じdata directory契約を使う。
- YAMLはschema version 1、原子置換、可能な環境で所有者read/writeのみ、破損した既存fileを自動上書きしない条件を試験した。

### [FACT] Wave 3 DUMMY-KUMA

- Hueyの永続queueを`queue.sqlite3`、観測正本を`kuma.sqlite3`へ分離した。
- FastMCPとは独立するforeground consumer entrypointとして`kuma800-worker`を追加した。
- macOS／Windows共通baselineはHueyの`thread` worker 2本である。worker processとFastMCP processを分離する設計であり、各taskを個別processへ隔離したという意味ではない。
- testは別Python processでtaskをenqueueし、別consumer processがDUMMY-KUMAを観測DBへappendするところまで通した。
- DUMMY-KUMAは外部networkへ接続しない。同じevent ID／内容hashを返し、二回のfetch runから観測一件へ冪等収束した。
- sourceごとに`STARTED`を一件だけ許すpartial unique indexを追加した。
- consumer起動時、15分より古い`STARTED`を`STALE`へ遷移させる回収処理を追加した。

### [FACT] Wave 4 FastMCP

- FastMCP 3.4.7をexact pinした。公式versioning方針がminor releaseでも破壊変更の可能性を明記しているため、更新はlock更新と回帰試験を伴う明示作業にする。
- MCP serverは`127.0.0.1`固定のStreamable HTTPとして別process起動する。portだけ`KUMA800_MCP_PORT`で変更できる。
- in-memory MCP transportでtool schemaとstructured resultを検証し、別processのloopback HTTP endpointからtool一覧を取得した。
- 観測面は固定SELECTだけを公開する。任意SQL、観測INSERT／UPDATE／DELETEはtoolにしない。
- scrape requestはHueyへenqueueしてtask IDを返し、同じMCP request内で完了を待たない。
- 利用者YAMLのlist、get、upsert、deleteをMCP protocol越しに往復した。

### [FACT] Wave 5 部分復旧

- 未知source IDはqueueへ入れる前にMCP requestを拒否し、見かけ上の`QUEUED`を返さない。
- 古い`STARTED`を残した状態から別consumer processを起動し、起動時に`STALE`へ回収するprocess smokeを通した。
- test用`queue.sqlite3`を削除・再作成しても、別fileの`kuma.sqlite3`にある観測一件が残ることを確認した。
- retry、backoff、thread hang、Huey result retentionは未実装のため、Wave 5全体はgreenではない。

### [FACT] Wave 6 macOS launchd probe

- `plistlib`でworkerとMCPのLaunchAgent plistを別々に生成するadapterを追加した。
- `RunAtLoad`と`KeepAlive`を指定し、両processへ同じ`KUMA800_DATA_DIR`を渡す。stdout／stderrはrole別fileへ分離する。
- 初回probeでは、CLIがvenv Pythonのsymlinkを`.resolve()`してHomebrew Python本体へ変換した。その結果、launchdは`ModuleNotFoundError: No module named 'kuma800'`でexit 1と再起動を繰り返した。
- 初回probeの両serviceをbootoutし、Python executableだけsymlinkを解決せず絶対path化するよう修正した。回帰試験`test_cli_preserves_virtualenv_python_symlink`を追加した。
- 修正版plistは`plutil -lint`で2本ともOKだった。
- 修正版を一時的に`gui/501`へbootstrapし、worker PID 16098とMCP PID 16105が別processで`running`、`last exit code = (never exited)`となることを確認した。
- `http://127.0.0.1:8765/mcp`へ到達し、HTTP HEADは405と`Allow: GET, POST, DELETE`を返した。これはendpoint不在ではなく、MCP endpointがHEADを許可しない応答である。
- launchd配下のHueyへDUMMY taskをenqueueし、観測一件、`SUCCEEDED` fetch run二件を確認した。periodic taskと手動taskが同じ観測へ冪等収束した。
- probe後はworkerとMCPをbootoutし、`launchctl print`で両labelが未登録であることを確認した。常設導入は行っていない。

このprobeは起動、処理、停止の短時間receiptである。login起動、restart、sleep／resume、reboot、長時間常駐の証明ではない。

### [FACT] Wave 7 山形県CSV

- 2026-08-11の再確認時、山形県[「クマに関する情報」ページ](https://www.pref.yamagata.jp/050011/kurashi/shizen/seibutsu/about_kuma/kuma_yamagata_top.html)は更新日2026-08-04、[リンク先CSV](https://www.pref.yamagata.jp/documents/2414/20260802_kemonote-cleaned.csv)は令和8年8月2日現在・179KBと表示していた。
- adapterは固定CSV URLを埋め込まず、県ページ内の`YYYYMMDD_kemonote-cleaned.csv`から最新日付を選ぶ。これは山形県の公式APIやKUMA800との公式連携ではなく、県公式web公開面を読む非公式adapterである。
- HTTP fetcherはHTTPSと`www.pref.yamagata.jp`をallowlistにし、redirect 5回、timeout 15秒、展開後1MiB、Content-Typeを制限する。
- live CSVはUTF-8 BOM付き、182,679 bytes、789 records、20 columnsだった。artifact SHA-256は`5053b064b5675389247dccf35c9f1551b901d224cf9cdf4c58d4e4cb82338138`。
- schemaには投稿日、group名、user名、緯度、経度、地域mesh、目撃日、時間帯、地名、市街地分類、周辺環境、頭数、個体サイズ、備考が含まれる。
- group名とuser名は`source_event_id`、row `content_hash`、`original_text`から除外する。公開CSVに存在することと、KUMA800の安全queryに必要であることを同一視しない。
- event IDは投稿日、座標、目撃日・時間、地名から安定生成する。内容hashは公開者識別欄を除いたrow全体から生成するため、同一eventの備考等が変化すれば別内容として追記できる。
- 現行CSVを一時data directoryへ実ingestした。1回目はcandidate 789／insert 789、2回目はcandidate 789／insert 0、fetch runは2件とも`SUCCEEDED`となった。
- raw CSVとlive SQLiteはrepositoryへ追加していない。offline fixtureは同じheaderを持つ架空2行だけである。
- schedulerは毎時17分に県ページを確認する。これは暫定負荷設定であり、行政の更新頻度、鮮度SLO、事故時の即時性を保証しない。

## 自動補完した仕様穴

### [INTERPRETATION] runtime path

`KUMA800_DATA_DIR`があればそれを使用し、なければ`platformdirs`のOS標準user data directoryを使う。queue、観測正本、利用者YAMLを同じdirectory内の別fileとして扱うため、service managerは一つの環境変数だけを固定すればよい。repository内は既定保存先にしない。

### [INTERPRETATION] DUMMY pollとstale閾値

- DUMMY scheduler：5分間隔
- stale回収：起動時に15分超の`STARTED`

これはSeason 1のprocess pathを検証する暫定値であり、実データ源の取得間隔、安全上の鮮度保証、障害復旧SLOではない。山形県CSV adapter導入時は、公開頻度、取得時間、timeout、backoffの実測に合わせてsource別設定へ移す。

### [INTERPRETATION] YAML同時writer

Season 1は単一FastMCP control-plane processだけが`users.yaml`を書く前提なので、同一directory内の原子置換までを実装した。複数MCP processや外部editorとの同時更新を許す段階では、世代番号またはfile lockを追加する。現段階で不要なlock daemonを増やさない。

## 検証条件

FastMCP参照資料：

- [公式Installation](https://gofastmcp.com/getting-started/installation)
- [公式Running Your Server](https://gofastmcp.com/deployment/running-server)
- [公式Client Transports](https://gofastmcp.com/clients/transports)

実行command：

```console
uv run --frozen ruff format src tests
uv run --frozen ruff check .
uv run --frozen mypy
uv run --frozen pytest -m "not live and not physical"
git diff --check
```

Wave 3時点の結果：16 tests passed、branch coverage込み総合81.49%。process smokeを含む。

Wave 4はFastMCP 3.4.7を追加し、in-memory transportとloopback HTTP process smokeを追加した。commit `59d4e61`のLinux CIはsuccess（[Actions run 31482402623](https://github.com/saitoomituru/KUMA800/actions/runs/31482402623)）。

Wave 5部分追加後のsandbox内全suiteは20 passed、1 skipped、branch coverage込み81.40%。skipはsandboxがloopback bindを`EPERM`で拒否した一件だけで、同じHTTP process smokeをsandbox外で単独実行すると1 passed。実データ、launchd、Windows実機はこの結果に含まれない。

Wave 6 launchd adapter追加後は23 passed、1 skipped、branch coverage込み81.12%。加えてmacOS実機で一時bootstrap、DUMMY task処理、bootoutを手動probeした。

Wave 7追加後のoffline suiteは34 passed、1 skipped、1 live deselected、branch coverage込み82.80%。明示live testは別実行で1 passed、実ingest二回で789件への冪等収束を確認した。

## 未完了と停止条件

- task途中でconsumerをhard stopした後のstale回収E2E
- retry、backoff、thread hang、Huey result retention
- macOS login起動、restart、sleep／resume、reboot、長時間運転
- Windows service adapterと実機acceptance
- 山形県CSVの更新頻度・schema変更・消失を継続観測する運用試験

このレシートは上記をgreenへ昇格しない。Windows物理daemonの判断はIssue #4の物資調達・実機試験まで`RESOURCE-WAIT`を維持する。
