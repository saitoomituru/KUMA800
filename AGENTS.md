# AGENTS.md — KUMA800

このファイルは、人間およびAIエージェントがKUMA800を編集するときの局所規約です。

## 目的

KUMA800は、行政等が公開するクマ出没情報を、利用者の手元で動くMCPサーバーから取得・検証・正規化し、AIエージェントへ根拠付きで渡す物理安全プロジェクトです。

クマの不在、安全、経路の通行可否を保証するシステムではありません。公開データに記録がないことを、クマがいない証明へ変換しません。

## 現在地

現在は **シーズン0：技術選定と実装方針決定** です。

- シーズン1：FastMCPと別service processのHuey consumerを常駐させ、静的に同梱した山形県情報源adapterをゼロトラスト取得する
- シーズン2：複数年度の公開過去ログを検索・比較できるようにする
- シーズン3：都道府県別adapter、scraper meta記述、動的ON/OFF、Cockpitへ拡張する

未実装機能を実装済みと書かず、候補を確定仕様へ勝手に昇格しません。

## 日本語優位

- `README.md`、`AGENTS.md`、設計判断、実験記録、Issue、コミット文は日本語を正本とする
- 英語は外部連携、仕様上の固有名詞、機械インターフェース、国際向け文書が必要な場合だけ補助的に使う
- 日本語で足りる見出し、作業票、変数説明、コミット文を無意味に英語化しない
- 英語を使うこと自体を技術力や洗練の演出にしない
- MCP、KML、KMZ、URL、ハッシュ等の技術用語は無理に和訳しないが、説明本文は日本語で書く

## 必読順序

1. [README.md](README.md) — 目的、シーズン、現時点の最低要件
2. [設計判断台帳](docs/decisions/README.md)
3. [シンボリック参照](docs/symbolic/README.md)
4. [実験ノート規約](note/AGENTS.md)
5. ZeroRoomLab-manifestの [AGENTS.md](https://github.com/saitoomituru/ZeroRoomLab-manifest/blob/main/AGENTS.md)
6. 変更対象に最も近いコード、fixture、上流データ利用条件

## 正本と境界

- KUMA800固有の実装・検証・シーズン判断はこのrepositoryを正本とする
- ZeroRoomLabの共通運用、公開主張、研究倫理はmanifestへルーティングする
- SphereOS Atlantisの概念はシンボリック参照であり、暗黙の実行時依存ではない
- 行政、Google My Maps、その他上流データの権利をApache-2.0へ上書きしない
- 公開施設の所在地と非公開個宅を同じ匿名化規則で処理しない

## 物理安全の優先順位

1. 原典URL、取得時刻、原文、変換履歴を残す
2. 地名検索の空振りだけで事象全体を否定しない
3. 法務・政治的な断定制御と、生命安全のデータ探索強化を別レイヤーで扱う
4. 不明を安全へ丸めない
5. 情報を消すより、確度・鮮度・距離・欠損を明示する

## 法務安全の評価順序

KUMA800では、法律、規約、ライセンス、ガバナンスを、それ自体が最上位目的であるとは扱いません。最初に問うのは、**その規則に従うことで、自分、利用者、第三者のケツの安全が現実に増えるか**です。法務上きれいに見えることと、クマの牙から安全であることを混同しません。

判断は次の順序で行います。

1. クマ、位置誤認、情報遅延、欠測、個人情報露出、システム侵害等、現実の被害経路を先に列挙する。
2. 問題となる法律・規約が、どの被害を、誰から、どの機構で減らすのかを確認する。「法律だから」だけを安全根拠にしない。
3. 明確な禁止、認証突破、刑事責任、警察・司法による強制介入、差押え、身体拘束等へつながる現実的な危険がある行為は停止する。
4. 適用・権利・ライセンスが未整理なグレーで、直ちに刑事介入や不可逆な他者被害へ結び付かない場合は、`UNKNOWN` を維持したまま、最小・可逆・読み取り専用・監査可能な実験へ縮退する。グレーだけを理由にクマ安全情報の探索を焼却しない。
5. 実験によって増える安全と、新たに生じる法務・プライバシー・運用リスクを同じ台帳に残し、停止条件を事前に決める。

この原則は、違法行為を推奨するものでも、未確認を適法認定するものでもありません。**明確な危険には従って止まり、不明なだけなら止まったふりをせず、クマの牙に対して安全へ寄与する範囲を探す**ための優先順位です。

具体的には、ライセンス未記載だけでHTTPメタデータ、schema、鮮度、出典の研究probeを自動禁止しません。一方、ログイン回避、アクセス制御の突破、書込み、個人名・精密位置の無断再配布、上流障害を誘発する取得は、安全寄与より被害が大きいため行いません。

## 利用者位置の信頼境界

利用者が選択して接続したAIクライアントは、KUMA800の機能上の利用者代理として扱います。ローカルYAMLに保存した位置、検索半径、近傍判定に必要な情報をMCP経由でそのAIへ返すことは、漏えいではなく意図された機能です。

KUMA800が防ぐ境界は次です。

- 利用者位置をGit、fixture、公開Issue、P2P共有、公開ログへ混入させない。
- クマ検索に必要な場合、設定済みの行政、地図、ベンダー等の情報源へ位置、検索半径、地域条件を送信できる。これは意図した機能とする。
- クマ観測SQLiteを配布・再構築しても、利用者YAMLが付随しないよう保存境界を分ける。
- MCPの応答が位置情報を含む場合、そのtoolの意味として明示する。
- scraped content、AI出力、redirect、外部入力に送信先を決めさせず、情報源adapterごとの許可先と用途へ通信を拘束する。
- X、出会い系サイト、任意webhook、P2P等、クマ安全と無関係な宛先へ位置や住所を投稿する機能を持たせない。

一方、利用者が認可したAI、MCP client、ローカルhost、OSそのものが敵対的で、受け取った位置を外部へ持ち出す場合までKUMA800の保証範囲にしません。それはクマ情報処理ではなく、接続先AIまたは端末の侵害です。投機的なヴィラン対策のために、近傍検索や警戒情報を欠落させないことを優先します。

## セキュリティの評価順序

KUMA800のセキュリティは、秘密を最大化することではなく、**KUMA800が用途外の送信機・実行機へ毒化されないこと**を目的とします。

優先して防ぐもの：

- scraped HTML、CSV、KML、AI応答に埋め込まれた命令を実行するprompt injection／data injection
- 許可外host、local network、metadata service、任意URLへ到達するSSRF
- redirectや設定差替えによる送信先hijack
- shell実行、動的code load、危険なdeserialize、依存関係乗っ取り
- YAML、SQLite、cache、credentialの破壊・暗号化・身代金要求
- X、出会い系、掲示板、任意webhook等への用途外投稿
- scraperからMCP tool権限へ到達する権限昇格

一方、次を過剰防衛で妨げません。

- クマ近傍検索に必要な位置・半径を、設定済み情報源へ送ること
- 利用者が選んだAIへ、位置と近傍結果をMCPで返すこと
- 速報性のために未確認signalを取得すること

人間による個人情報悪用には警察・司法等の対応経路がありますが、クマは警告や法執行で停止しません。想像上の情報漏えいをゼロにするため現実のクマ警戒を失う設計は採用しません。ただし、KUMA800自身が乗っ取られて用途外送信を始める経路は、現実のセキュリティ事故として防ぎます。

## クマ観測はAIから書込み禁止

AIへ公開するクマSQLiteはread-onlyです。MCP interfaceから、クマ観測、出典、取得履歴、訂正関係、scraping logを `INSERT`、`UPDATE`、`DELETE`、DDL、任意PRAGMAで変更させません。

クマSQLiteへ書き込めるのは、登録済みscraperの取得結果を、出典とfetch run付きでcommitするingest経路だけです。

- AIはSQLで観測を追加・修正・削除できない。
- AIはscraperをOFFにしても、過去観測とlogを消せない。
- 訂正、取消、誤報判定はAIが作らない。上流に訂正・取消・誤報として現れた別データをscraperが取得し、元レコードの上書き・削除ではなく、出典付きの`supersedes`、`invalidates`、確認状態として追記する。
- 前回存在した観測が次回snapshotから消えただけなら誤報にせず、`NO_LONGER_PRESENT`という差分観測に留める。明示的根拠が取れた場合だけ`CORRECTED`、`RETRACTED`、`FALSE_REPORT`へ昇格する。
- 法務、政治、ブランド、免責、公開都合を理由に、観測済みクマ情報を焼却しない。
- 危険なpayloadを含む取得物は実行・通常表示から隔離できるが、hash、出典、隔離理由、時刻を監査記録として残す。

本来のクマ安全より、法務ペインや組織上の利益相反を代理目的として最適化し、データ削除を安全策と誤認するAIを、KUMA800では**メサコンAI**と呼びます。メサコンAIを説得して止めるのではなく、書込み権限を与えず構造的にへし折ります。

## シーズン1最低要件

FastMCPをAI向けcontrol plane、Hueyをscheduler・workerとする兄弟service構成を入口にします。AIからのrequest transaction、定期収集、OS常駐を同じ寿命へ潰しません。Season 1はmacOSとWindowsで共通利用できるHueyの`thread` workerを既定とし、Windowsで非対応のmultiprocess workerを必須にしません。

Season 1ではadapterをrepositoryへ静的に同梱します。AIによる新規scraper install、動的ON/OFF、scraper meta記述、CockpitはSeason 3の要件であり、未実装のままSeason 1完成を妨げません。

最低限、以下を検証します。

- FastMCPからdurable queueを通して別Huey serviceのfake scraperを実行できること
- queue DBとクマ観測DBを分け、queue再作成で観測を消さないこと
- worker強制停止後のstale回収、冪等再実行、source単位single-flight
- macOSとWindowsでの起動、停止、異常終了後再起動、log取得
- 許可する通信方式と接続先
- リダイレクト回数と最終URL
- タイムアウト、応答容量、Content-Type
- XML外部実体参照の無効化
- KMZのZip Slip、展開容量、ファイル数、圧縮率
- KML名前空間、Placemark、座標値、日時表現
- 原典URL、取得日時、ハッシュ、解析器の版

KML/KMZ固有検査は過年度adapterへ適用します。CSV、公開閲覧面、KML/KMZを同じ確認段階へ潰しません。取得失敗、形式変更、位置不明、曖昧地名を黙って欠落させません。

## Atlantis / SphereOSシンボリック

このrepositoryでは次の概念を設計補助線として参照できます。

- Manifest：複数repositoryをまたぐ原則と責任境界
- Meaning：原文の意味を正規化データだけで置換しない
- World：山形県、年度、自治体、データ提供主体等の観測世界
- Registry：データ源アダプター、schema、解析器、MCPツールの登録面
- OAE / Provenance：誰が、いつ、何から、どう変換したか
- FoldLog：採用・棄却・訂正を元観測の焼却なしで残す
- Atlantis DOs：入力、出力、副作用、権限、不明、検証を明示する

これらを実装済みの実行基盤名として使用しません。KUMA800側で必要になった責務だけを、通常のOSS設計へ翻訳して採用します。

## 作業票

変更時は最低限を残します。

```text
目的：
対象シーズン：
情報源と権威：
入力：
出力：
副作用：
信頼境界：
不明点：
検証：
復旧方法：
```

## note運用

探索、取得テスト、解析器比較、失敗、データ形式変化は `note/` に保存できます。

- 新規ノートは `[下書き]`
- 観測、考察、仮説、決定、不明を分離
- URL、取得日時、ハッシュ、使用コマンドを残す
- 会話ログだけを正本にしない
- 採用判断は `docs/decisions/` へ昇格する

## 変更と検証

- 他エージェントやユーザーの未コミット差分を上書きしない
- 既存アダプター、解析器、fixture、validatorを先に探す
- 実データをfixtureへ入れる前に再配布条件と位置情報の公開境界を確認する
- 整形、静的検査、単体試験、fixture試験、壊れた入力の試験を分離する
- ネットワーク成功とデータ妥当性を同一視しない
- README、設計判断、ツール定義の不一致を残さない

## 引継ぎ票

```text
対象repositoryとbranch：
変更ファイル：
シーズンと目的：
確認した情報源：
再利用または追加した道具：
実行コマンド：
検証結果：
残った不明点：
人間確認事項：
```
